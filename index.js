/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import AscencioApp from './src/AscencioApp';

AppRegistry.registerComponent(appName, () => AscencioApp);
